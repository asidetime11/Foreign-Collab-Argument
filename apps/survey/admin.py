"""Survey response models stay hidden from the raw Django admin.

Researchers review and export these records through the custom research console,
where the data is grouped by participant instead of exposed as implementation
tables.
"""
